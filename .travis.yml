language: csharp
os: linux
dist: focal
mono: none
dotnet: 5.0.102
solution: FakeServer.sln
script:
  - dotnet restore
  - dotnet build
  - dotnet test FakeServer.Test/FakeServer.Test.csproj
before_install:
  - echo fs.inotify.max_user_watches=524288 | sudo tee -a /etc/sysctl.conf && sudo sysctl -p